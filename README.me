get_configs is util for getting and saving configs from switches and routers different vendors (Cisco, D-link, Erricsson)

File secrets.example need rename as secrets and edit it

Configs saving in config-auto dir by tftp protocol

Configs remain if it is new

The code uses the language 'expect'

